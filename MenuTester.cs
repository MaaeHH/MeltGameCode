using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MenuTester : MonoBehaviour
{
    /*[SerializeField] private List<MissionData> missionDatas;
    [SerializeField] private MissionMenu missionMenu;
    // Start is called before the first frame update
    void Start()
    {
        List<MissionInstance> asd = new List<MissionInstance>();
        foreach(MissionData missionData in missionDatas)
        {
            asd.Add(new MissionInstance(missionData));
        }

        missionMenu.SetMissions(asd);
        missionMenu.OpenMenu();
    }

    // Update is called once per frame
    void Update()
    {
        
    }*/
}
